# bash code block for .profile

TYPING_PROMPT_SYMBOL='$ '
PS1='\n\s_\v    docker ${WORKSPACE_NAME} php_composer \u ${TYPING_PROMPT_SYMBOL}'
# PS1='\n\s_\v    docker ${WORKSPACE_NAME} php_fpm \u ${TYPING_PROMPT_SYMBOL}'

# cd ~
cd "${IN_DOCKER_WORKSPACE_VOLUME}"

. "${IN_DOCKER_WORKSPACE_VOLUME}/env_dc_dinamique/.env_php"


